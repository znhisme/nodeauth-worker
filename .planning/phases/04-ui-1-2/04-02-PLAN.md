---
phase: 04-ui-1-2
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/features/share/shareTypes.ts
  - src/features/share/shareService.ts
  - src/features/share/shareService.test.ts
  - src/features/share/shareRoutes.ts
  - src/features/share/shareRoutes.test.ts
autonomous: true
requirements:
  - PH4-BE-02
  - PH4-SEC-01
must_haves:
  truths:
    - "Authenticated owners can batch-create share links for selected accounts through one owner-safe API call."
    - "Each successful batch row returns the raw public URL/token and raw access code exactly once."
    - "Batch partial failures do not reveal inaccessible account existence, owner IDs, token hashes, access-code hashes, or internal diagnostics."
  artifacts:
    - path: "src/features/share/shareTypes.ts"
      provides: "Batch share request/response DTO contracts"
      exports: ["CreateSharesBatchInput", "OwnerShareBatchCreatedView"]
    - path: "src/features/share/shareService.ts"
      provides: "Owner batch share creation semantics using latest-share-wins per item"
      exports: ["ShareService.createSharesForOwnerBatch"]
    - path: "src/features/share/shareRoutes.ts"
      provides: "POST /api/share/batch owner route"
      contains: "share.post('/batch'"
    - path: "src/features/share/shareRoutes.test.ts"
      provides: "Route tests for batch response shape and privacy allowlists"
      contains: "/api/share/batch"
  key_links:
    - from: "src/features/share/shareRoutes.ts"
      to: "src/features/share/shareService.ts"
      via: "POST /api/share/batch calls createSharesForOwnerBatch with ownerId from authMiddleware user"
      pattern: "createSharesForOwnerBatch\\("
    - from: "src/features/share/shareService.ts"
      to: "src/features/share/shareService.ts"
      via: "batch implementation delegates each item to createShareForOwner so latest-share-wins and one-time secret rules are shared"
      pattern: "createShareForOwner\\("
---

<objective>
Add an owner-safe batch share API for selected My Accounts rows.

Purpose: The requested bulk Share button needs backend semantics that validate ownership per selected account, replace older active links per item, preserve one-time secret delivery for successful rows, and keep partial failures privacy-safe.
Output: Batch DTOs, service method, route, and tests for safe batch sharing.
</objective>

<execution_context>
@/home/znh/.codex/get-shit-done/workflows/execute-plan.md
@/home/znh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-1-2/04-RESEARCH.md
@.planning/phases/04-ui-1-2/04-UI-SPEC.md
@.planning/phases/04-ui-1-2/04-VALIDATION.md
@.planning/phases/04-ui-1-2/04-PATTERNS.md
@docs/share-link-security-contract.md
@src/features/share/shareTypes.ts
@src/features/share/shareService.ts
@src/features/share/shareService.test.ts
@src/features/share/shareRoutes.ts
@src/features/share/shareRoutes.test.ts

<interfaces>
From `src/features/share/shareRoutes.ts`:
```typescript
share.post('/', authMiddleware, async (c) => {
    const user = c.get('user');
    const ownerId = user.email || user.id;
    const body = await c.req.json().catch(() => ({}));
    const publicOrigin = c.env.NODEAUTH_PUBLIC_ORIGIN || new URL(c.req.url).origin;
});
```

From `src/features/share/shareService.ts`:
```typescript
async createShareForOwner(input: CreateShareInput): Promise<OwnerShareCreatedView>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define batch DTOs and service behavior</name>
  <files>src/features/share/shareTypes.ts, src/features/share/shareService.ts, src/features/share/shareService.test.ts</files>
  <read_first>
    - src/features/share/shareTypes.ts
    - src/features/share/shareService.ts
    - src/features/share/shareService.test.ts
    - .planning/phases/04-ui-1-2/04-UI-SPEC.md
    - .planning/phases/04-ui-1-2/04-PATTERNS.md
  </read_first>
  <behavior>
    - `createSharesForOwnerBatch()` processes `vaultItemIds` in request order and returns `successes` and `failures`.
    - Success rows include `requestIndex` and `share: OwnerShareCreatedView`, including one-time `rawToken` and `rawAccessCode`.
    - Failure rows include only `requestIndex` and `error: "could_not_create_share"`.
  </behavior>
  <action>In `shareTypes.ts`, add `CreateSharesBatchInput extends Omit&lt;CreateShareInput, 'vaultItemId'&gt; { vaultItemIds: string[] }`, `OwnerShareBatchSuccessView { requestIndex: number; share: OwnerShareCreatedView }`, `OwnerShareBatchFailureView { requestIndex: number; error: 'could_not_create_share' }`, and `OwnerShareBatchCreatedView { successes: OwnerShareBatchSuccessView[]; failures: OwnerShareBatchFailureView[] }`. In `ShareService`, add `createSharesForOwnerBatch(input: CreateSharesBatchInput): Promise&lt;OwnerShareBatchCreatedView&gt;`. Use `const batchNow = input.now ?? Date.now()` and call `createShareForOwner({ ownerId: input.ownerId, vaultItemId, ttlSeconds: input.ttlSeconds, expiresAt: input.expiresAt, now: batchNow, publicOrigin: input.publicOrigin })` for each `vaultItemIds` entry. Catch per-item failures and append `{ requestIndex, error: 'could_not_create_share' }`; do not include failed `vaultItemId`, owner ID, service/account labels, exception messages, stack traces, token hashes, access-code hashes, or credential data in failure rows. Add service tests for all-success, mixed success/failure, request-order indices, and failure privacy.</action>
  <verify>
    <automated>cd backend && npm test -- ../src/features/share/shareService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "CreateSharesBatchInput|OwnerShareBatchCreatedView|OwnerShareBatchFailureView" src/features/share/shareTypes.ts` returns matches.
    - `rg -n "createSharesForOwnerBatch" src/features/share/shareService.ts src/features/share/shareService.test.ts` returns matches in both files.
    - `rg -n "could_not_create_share" src/features/share/shareService.ts src/features/share/shareService.test.ts` returns matches in both files.
    - `cd backend && npm test -- ../src/features/share/shareService.test.ts` exits 0.
  </acceptance_criteria>
  <done>Batch service behavior reuses single-share latest-share-wins creation and returns one-time secrets only for successful owner-scoped rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose POST /api/share/batch route</name>
  <files>src/features/share/shareRoutes.ts, src/features/share/shareRoutes.test.ts</files>
  <read_first>
    - src/features/share/shareRoutes.ts
    - src/features/share/shareRoutes.test.ts
    - src/features/share/shareTypes.ts
    - .planning/phases/04-ui-1-2/04-VALIDATION.md
    - .planning/phases/04-ui-1-2/04-PATTERNS.md
  </read_first>
  <behavior>
    - `POST /api/share/batch` uses `authMiddleware` and derives `ownerId` only from `c.get('user').email || c.get('user').id`.
    - Invalid `vaultItemIds` returns HTTP 400 before calling the service.
    - Valid requests return `{ success: true, result }` and route tests prove response allowlists for successes and failures.
  </behavior>
  <action>Add `share.post('/batch', authMiddleware, async (c) => { ... })` before `share.post('/public/:token/access', ...)`. Parse JSON with `await c.req.json().catch(() =&gt; ({}))`. Validate `body.vaultItemIds` is a non-empty array of non-empty strings; return `c.json({ success: false, error: 'vaultItemIds must be a non-empty array of strings' }, 400)` otherwise. Reject more than 50 IDs with `c.json({ success: false, error: 'vaultItemIds cannot exceed 50' }, 400)`. Derive `publicOrigin` as `c.env.NODEAUTH_PUBLIC_ORIGIN || new URL(c.req.url).origin`; forward only finite `ttlSeconds` and `expiresAt`; call `service.createSharesForOwnerBatch({ ownerId, vaultItemIds: body.vaultItemIds, ttlSeconds, expiresAt, publicOrigin })`; return `c.json({ success: true, result })`. Extend route mocks with `createSharesForOwnerBatch`. Add tests that attacker-supplied `ownerId` is ignored, invalid arrays return 400, partial failures are generic, success rows allow `rawToken`/`rawAccessCode`, and no row exposes `ownerId`, `tokenHash`, `accessCodeHash`, `session`, `backup`, `password`, `secret`, `seed`, `otpauth`, stack traces, or inaccessible account labels.</action>
  <verify>
    <automated>cd backend && npm test -- ../src/features/share/shareRoutes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "share\\.post\\('/batch'" src/features/share/shareRoutes.ts` returns a match before the public route.
    - `rg -n "vaultItemIds must be a non-empty array of strings|vaultItemIds cannot exceed 50" src/features/share/shareRoutes.ts src/features/share/shareRoutes.test.ts` returns matches.
    - `rg -n "createSharesForOwnerBatch" src/features/share/shareRoutes.ts src/features/share/shareRoutes.test.ts` returns matches in both files.
    - `cd backend && npm test -- ../src/features/share/shareRoutes.test.ts` exits 0.
  </acceptance_criteria>
  <done>`POST /api/share/batch` is authenticated, owner-scoped, bounded, and privacy-allowlisted for batch successes and partial failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner client -> batch route | Authenticated owner submits multiple selected vault item IDs and optional TTL/expiration. |
| batch route -> share service | Route must not trust client owner IDs or expose internal service errors. |
| share service -> owner UI | Service returns one-time raw secrets only for successful rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `createSharesForOwnerBatch` | mitigate | Delegate every successful row to `createShareForOwner`, which already enforces latest-share-wins from Plan 01. |
| T-04-02 | Information Disclosure | batch success response | mitigate | Return raw URL/token and access code only inside success rows from the creation response; list/detail endpoints remain metadata-only. |
| T-04-03 | Information Disclosure | batch partial failure response | mitigate | Failure rows contain only `requestIndex` and `error: "could_not_create_share"`; tests assert no inaccessible labels, owner IDs, internal IDs beyond success DTOs, hashes, secrets, or diagnostics. |
| T-04-05 | Tampering | frontend generated chunks | accept | This plan adds backend API semantics only and does not edit `frontend/dist/**`; UI execution remains gated in Plan 03. |
</threat_model>

<verification>
Run targeted route/service tests:
`cd backend && npm test -- ../src/features/share/shareService.test.ts ../src/features/share/shareRoutes.test.ts`
</verification>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 4 | My Accounts supports batch sharing for selected accounts | 04-02 | COVERED | Adds backend batch API semantics that the toolbar UI can call after frontend source restoration. |
| REQ | PH4-BE-02 | Owner-safe batch share API or equivalent backend semantics | 04-02 | COVERED | `POST /api/share/batch` returns per-row successes/failures. |
| REQ | PH4-SEC-01 | One-time raw public URL/access code per success and safe partial failure shape | 04-02 | COVERED | Success rows include one-time secrets; failure rows are generic and privacy allowlisted. |
| RESEARCH | batch endpoint recommendation | Backend batch is safer than client loop for consistent replacement and partial-failure reporting | 04-02 | COVERED | Service delegates per item to single-share creation and route bounds the request. |
| CONTEXT | none | No Phase 4 CONTEXT.md decisions exist | 04-02 | COVERED | Verified by init context; no deferred idea is implemented. |
</source_audit>

<success_criteria>
- Batch owner share creation works through `POST /api/share/batch`.
- Successful rows preserve one-time secret delivery.
- Partial failures are safe and generic.
- Targeted service and route tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-1-2/04-02-SUMMARY.md`.
</output>

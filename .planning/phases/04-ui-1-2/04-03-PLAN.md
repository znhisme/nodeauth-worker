---
phase: 04-ui-1-2
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - backend/dist/worker/worker.js
  - backend/dist/worker/worker.js.map
  - backend/dist/docker/server.js
  - backend/dist/docker/server.js.map
  - backend/dist/netlify/api.mjs
  - backend/dist/netlify/api.mjs.map
  - .planning/phases/04-ui-1-2/04-FRONTEND-SOURCE-GATE.md
autonomous: true
requirements:
  - PH4-BUILD-01
  - PH4-UI-01
  - PH4-UI-02
  - PH4-SEC-01
must_haves:
  truths:
    - "Worker, Docker, and Netlify backend bundles are regenerated from source after Phase 4 backend changes."
    - "The current checkout does not hand-edit generated Vite frontend chunks as the primary UI implementation path."
    - "Manage Shares, My Accounts bulk Share, select-all/group-select-all, one-time dialog, and privacy UI requirements are explicitly gated on restored editable Vue source."
  artifacts:
    - path: "backend/dist/worker/worker.js"
      provides: "Cloudflare Worker bundle containing latest-share-wins and batch route behavior"
      contains: "createSharesForOwnerBatch"
    - path: "backend/dist/docker/server.js"
      provides: "Docker backend bundle containing latest-share-wins and batch route behavior"
      contains: "createSharesForOwnerBatch"
    - path: "backend/dist/netlify/api.mjs"
      provides: "Netlify backend bundle containing latest-share-wins and batch route behavior"
      contains: "createSharesForOwnerBatch"
    - path: ".planning/phases/04-ui-1-2/04-FRONTEND-SOURCE-GATE.md"
      provides: "Executable gate documenting editable frontend source absence and exact UI implementation contract"
      contains: "Manage Shares"
  key_links:
    - from: "src/features/share/shareRoutes.ts"
      to: "backend/dist/worker/worker.js"
      via: "cd backend && npm run build:worker"
      pattern: "createSharesForOwnerBatch"
    - from: ".planning/phases/04-ui-1-2/04-UI-SPEC.md"
      to: ".planning/phases/04-ui-1-2/04-FRONTEND-SOURCE-GATE.md"
      via: "source gate copies required UI surfaces and forbids generated chunk patching"
      pattern: "frontend/dist"
---

<objective>
Regenerate backend runtime bundles and close the frontend source gate explicitly.

Purpose: Phase 4 backend behavior must ship in every supported backend runtime, while requested Vue UI work must not be implemented by hand-editing generated Vite chunks when editable frontend source is absent.
Output: Refreshed Worker/Docker/Netlify bundles plus a frontend source gate artifact that blocks UI implementation until maintainable Vue source exists.
</objective>

<execution_context>
@/home/znh/.codex/get-shit-done/workflows/execute-plan.md
@/home/znh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-1-2/04-RESEARCH.md
@.planning/phases/04-ui-1-2/04-UI-SPEC.md
@.planning/phases/04-ui-1-2/04-VALIDATION.md
@.planning/phases/04-ui-1-2/04-PATTERNS.md
@.planning/source-provenance.md
@backend/package.json
@backend/scripts/build-worker.js
@backend/scripts/build-docker.js
@backend/scripts/build-netlify.js
@frontend/dist/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run backend validation and regenerate runtime bundles</name>
  <files>backend/dist/worker/worker.js, backend/dist/worker/worker.js.map, backend/dist/docker/server.js, backend/dist/docker/server.js.map, backend/dist/netlify/api.mjs, backend/dist/netlify/api.mjs.map</files>
  <read_first>
    - backend/package.json
    - backend/scripts/build-worker.js
    - backend/scripts/build-docker.js
    - backend/scripts/build-netlify.js
    - .planning/phases/04-ui-1-2/04-VALIDATION.md
    - src/features/share/shareService.ts
    - src/features/share/shareRoutes.ts
  </read_first>
  <action>Run `cd backend && npm test -- ../src/features/share/shareService.test.ts ../src/features/share/shareRoutes.test.ts`, then `cd backend && npm test`. Regenerate supported backend bundles with exactly `cd backend && npm run build:worker && npm run build:docker && npm run build:netlify`. Do not run `npm run build:vercel` because `backend/scripts/build-vercel.js` is absent unless that script is restored in the same execution. After builds, run `node scripts/restore_backend_source_from_sourcemaps.js --verify` and generated-output assertions for `revokeActiveForOwnerVaultItem`, `createSharesForOwnerBatch`, `share_inaccessible`, `Cache-Control`, and `Referrer-Policy` across Worker, Docker, and Netlify bundles.</action>
  <verify>
    <automated>cd backend && npm test -- ../src/features/share/shareService.test.ts ../src/features/share/shareRoutes.test.ts && npm test && npm run build:worker && npm run build:docker && npm run build:netlify</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && npm test -- ../src/features/share/shareService.test.ts ../src/features/share/shareRoutes.test.ts` exits 0.
    - `cd backend && npm test` exits 0.
    - `cd backend && npm run build:worker && npm run build:docker && npm run build:netlify` exits 0.
    - `node scripts/restore_backend_source_from_sourcemaps.js --verify` exits 0.
    - `rg -n "revokeActiveForOwnerVaultItem|createSharesForOwnerBatch|share_inaccessible|Cache-Control|Referrer-Policy" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs` returns matches.
  </acceptance_criteria>
  <done>All supported backend runtime bundles are regenerated from source and contain Phase 4 latest-share-wins, batch route, and public privacy markers.</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend source gate artifact</name>
  <files>.planning/phases/04-ui-1-2/04-FRONTEND-SOURCE-GATE.md</files>
  <read_first>
    - .planning/phases/04-ui-1-2/04-UI-SPEC.md
    - .planning/phases/04-ui-1-2/04-RESEARCH.md
    - .planning/phases/04-ui-1-2/04-PATTERNS.md
    - frontend/dist/index.html
    - .planning/source-provenance.md
  </read_first>
  <action>Run `test -f frontend/package.json || test -d frontend/src`. If the command fails, create `.planning/phases/04-ui-1-2/04-FRONTEND-SOURCE-GATE.md` with: `status: blocked`, the exact command result, the statement `Do not hand-edit frontend/dist/assets/*.js as the primary UI implementation path`, and a checklist requiring restored editable Vue source before implementing `Manage Shares` left navigation, share management page, single-account create/regenerate/revoke, My Accounts toolbar order `Delete`, `Share`, `Cancel`, select-all-loaded/group-select-all behavior, online-only batch share, one-time result dialogs, and browser-storage/logging prohibitions from `04-UI-SPEC.md`. If editable source exists during execution, write `status: source-present` and list the discovered files from `find frontend -maxdepth 3 -type f \\( -name 'package.json' -o -path '*/src/*' \\) | sort`; do not edit generated `frontend/dist/**` in this plan.</action>
  <verify>
    <automated>test -f .planning/phases/04-ui-1-2/04-FRONTEND-SOURCE-GATE.md && rg -n "Do not hand-edit frontend/dist/assets/\\*\\.js|Manage Shares|Delete.*Share.*Cancel|Copy the link and access code now|Sharing requires a network connection" .planning/phases/04-ui-1-2/04-FRONTEND-SOURCE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-ui-1-2/04-FRONTEND-SOURCE-GATE.md` exists.
    - `rg -n "status: blocked|status: source-present" .planning/phases/04-ui-1-2/04-FRONTEND-SOURCE-GATE.md` returns a match.
    - `rg -n "Do not hand-edit frontend/dist/assets/\\*\\.js" .planning/phases/04-ui-1-2/04-FRONTEND-SOURCE-GATE.md` returns a match.
    - `git diff --name-only -- frontend/dist | rg "frontend/dist/assets/.*\\.js" -q` returns non-zero unless frontend builds legitimately regenerate files through restored source and package scripts.
  </acceptance_criteria>
  <done>The requested UI contract is explicitly blocked on editable frontend source restoration or confirmed source-present, without generated Vite chunk patching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source TypeScript -> generated backend bundles | Security behavior must be regenerated consistently for Cloudflare Workers, Docker, and Netlify. |
| generated frontend assets -> maintainer edits | Generated Vite chunks are not maintainable source and can hide security regressions. |
| owner UI -> browser storage/logs | Raw one-time share URLs/access codes must not be persisted or logged by restored-source UI work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | generated backend bundles | mitigate | Build Worker, Docker, and Netlify from source and grep for latest-share-wins markers in every bundle. |
| T-04-02 | Information Disclosure | restored-source UI one-time dialogs/storage | mitigate | Source gate copies UI-SPEC prohibitions for raw URL/access-code storage, logs, route/query params, IndexedDB, local storage, and service-worker cache. |
| T-04-03 | Information Disclosure | batch partial failure generated route | mitigate | Full targeted and backend tests run before builds; generated bundles must contain batch route markers. |
| T-04-04 | Information Disclosure | public recipient generated route | mitigate | Generated bundle assertions require `share_inaccessible`, `Cache-Control`, and `Referrer-Policy` markers. |
| T-04-05 | Tampering | `frontend/dist/assets/*.js` | mitigate | Create an explicit source gate and reject generated chunk patching as the primary UI implementation path. |
</threat_model>

<verification>
Overall phase gate:
`cd backend && npm test -- ../src/features/share/shareService.test.ts ../src/features/share/shareRoutes.test.ts`
`cd backend && npm test`
`cd backend && npm run build:worker && npm run build:docker && npm run build:netlify`
`node scripts/restore_backend_source_from_sourcemaps.js --verify`
</verification>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 4 | Left menu Manage Shares page and My Accounts Share button UI | 04-03 | COVERED | Covered as an explicit executable source gate because editable frontend source is absent; no generated chunk implementation is planned. |
| GOAL | Phase 4 | Backend latest-share-wins and batch share behavior available in supported runtimes | 04-03 | COVERED | Worker, Docker, and Netlify bundle regeneration is planned after backend source changes. |
| REQ | PH4-BUILD-01 | Regenerate backend bundles after source changes | 04-03 | COVERED | Uses `build:worker`, `build:docker`, and `build:netlify`; excludes missing Vercel script per validation requirement. |
| REQ | PH4-UI-01 | Share management UI contract from UI-SPEC | 04-03 | COVERED | Source gate lists exact UI surfaces and copy/actions to implement once editable source exists. |
| REQ | PH4-UI-02 | My Accounts select-all/group-select-all and toolbar Share button between Delete and Cancel | 04-03 | COVERED | Source gate requires toolbar order and selection semantics; frontend implementation is blocked by missing source. |
| REQ | PH4-SEC-01 | No public/privacy regression and no generated frontend chunk patching | 04-03 | COVERED | Bundle marker checks and frontend gate mitigate public error/privacy and maintainability threats. |
| RESEARCH | frontend source absence | No `frontend/package.json`, `frontend/src/**`, `.vue`, or source maps outside generated dist | 04-03 | COVERED | Plan writes `04-FRONTEND-SOURCE-GATE.md` instead of editing generated assets. |
| RESEARCH | build target constraint | Do not require `npm run build:vercel` while `backend/scripts/build-vercel.js` is absent | 04-03 | COVERED | Action explicitly runs Worker/Docker/Netlify builds only. |
| CONTEXT | none | No Phase 4 CONTEXT.md decisions exist | 04-03 | COVERED | Verified by init context; no deferred idea is implemented. |
</source_audit>

<success_criteria>
- Backend source changes are present in Worker, Docker, and Netlify bundles.
- Full backend tests pass.
- UI implementation is either blocked with a concrete source-restoration gate or marked source-present with discovered editable frontend files.
- No plan hand-edits generated `frontend/dist/assets/*.js` as the primary UI path.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-1-2/04-03-SUMMARY.md`.
</output>

---
phase: 01-foundation-and-security-primitives
plan: 04
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - scripts/validate_share_schema_alignment.js
  - .planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md
  - backend/dist/worker/worker.js
  - backend/dist/worker/worker.js.map
  - backend/dist/docker/server.js
  - backend/dist/docker/server.js.map
  - backend/dist/netlify/api.mjs
  - backend/dist/netlify/api.mjs.map
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
  - UX-04
must_haves:
  truths:
    - "Schema, migration, repository, tests, and generated backend outputs are aligned before Phase 1 is marked complete."
    - "Cloudflare Worker, Docker, and Netlify backend outputs contain the same share-link primitives generated from source."
    - "If source/build provenance is missing, the phase records the exact blocker and does not pass feature implementation verification."
  artifacts:
    - path: "scripts/validate_share_schema_alignment.js"
      provides: "Automated schema/source/output alignment validation"
    - path: ".planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md"
      provides: "Blocking validation result and exact command evidence"
    - path: "backend/dist/worker/worker.js"
      provides: "Regenerated Cloudflare Worker backend output"
    - path: "backend/dist/docker/server.js"
      provides: "Regenerated Docker backend output"
    - path: "backend/dist/netlify/api.mjs"
      provides: "Regenerated Netlify backend output"
  key_links:
    - from: "src/shared/db/schema/index.ts"
      to: "backend/dist/worker/worker.js"
      via: "npm --prefix backend run build:worker"
      pattern: "share_links"
    - from: "src/shared/db/migrator.ts"
      to: "backend/dist/docker/server.js"
      via: "npm --prefix backend run build:docker"
      pattern: "create_share_link_tables"
    - from: "backend/schema.sql"
      to: "scripts/validate_share_schema_alignment.js"
      via: "schema validator table/column checks"
      pattern: "share_rate_limits"
---

<objective>
Run the blocking schema/migration/build validation required after share schema modifications and before Phase 1 verification.

Purpose: Phase 1 cannot pass unless share schema and security primitives are source-backed, tested, and regenerated consistently across Cloudflare Worker, Docker, and Netlify backend outputs, or unless an exact source-provenance blocker is documented and dependent implementation is marked not executable.

Output: Alignment validation script, regenerated backend outputs, and a validation report with command evidence.
</objective>

<execution_context>
@/home/znh/.codex/get-shit-done/workflows/execute-plan.md
@/home/znh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-security-primitives/01-RESEARCH.md
@.planning/phases/01-foundation-and-security-primitives/01-UI-SPEC.md
@.planning/source-provenance.md
@docs/share-link-security-contract.md
@src/shared/db/schema/sqlite.ts
@src/shared/db/schema/mysql.ts
@src/shared/db/schema/pg.ts
@src/shared/db/schema/index.ts
@src/shared/db/migrator.ts
@backend/schema.sql
@backend/package.json
@backend/scripts/build-worker.js
@backend/scripts/build-docker.js
@backend/scripts/build-netlify.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Validate schema, migration, tests, and build output alignment</name>
  <files>scripts/validate_share_schema_alignment.js, .planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md, backend/dist/worker/worker.js, backend/dist/worker/worker.js.map, backend/dist/docker/server.js, backend/dist/docker/server.js.map, backend/dist/netlify/api.mjs, backend/dist/netlify/api.mjs.map</files>
  <read_first>
    - `scripts/validate_share_schema_alignment.js` if it already exists
    - `.planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md` if it already exists
    - `.planning/source-provenance.md`
    - `src/shared/db/schema/sqlite.ts`
    - `src/shared/db/schema/mysql.ts`
    - `src/shared/db/schema/pg.ts`
    - `src/shared/db/schema/index.ts`
    - `src/shared/db/migrator.ts`
    - `backend/schema.sql`
    - `backend/package.json`
    - `backend/scripts/build-worker.js`
    - `backend/scripts/build-docker.js`
    - `backend/scripts/build-netlify.js`
  </read_first>
  <action>Create `scripts/validate_share_schema_alignment.js` as a CommonJS Node script that reads `src/shared/db/schema/sqlite.ts`, `src/shared/db/schema/mysql.ts`, `src/shared/db/schema/pg.ts`, `src/shared/db/schema/index.ts`, `src/shared/db/migrator.ts`, `backend/schema.sql`, `backend/dist/worker/worker.js`, `backend/dist/docker/server.js`, and `backend/dist/netlify/api.mjs`. It must fail with exit code `1` if any file is missing or if any required string is absent. Required strings are `share_links`, `share_audit_events`, `share_rate_limits`, `token_hash`, `access_code_hash`, `vault_item_id`, `owner_id`, `expires_at`, `revoked_at`, `create_share_link_tables`, and `version: 13` where applicable; generated bundle checks must require `share_links`, `share_audit_events`, `share_rate_limits`, `share_item_inaccessible`, and `share_inaccessible`. Then run these commands in order: `node scripts/restore_backend_source_from_sourcemaps.js --verify`, `npm ci --prefix backend`, `npm --prefix backend test`, `npm --prefix backend run build:worker`, `npm --prefix backend run build:docker`, `npm --prefix backend run build:netlify`, and `node scripts/validate_share_schema_alignment.js`. Write `.planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md` with a `PASS` section only if every command exits `0`. If any source/build prerequisite is missing, write a `BLOCKED` section naming the exact missing file or failing command, and do not mark the plan complete.</action>
  <verify>
    <automated>node scripts/restore_backend_source_from_sourcemaps.js --verify</automated>
    <automated>npm ci --prefix backend</automated>
    <automated>npm --prefix backend test</automated>
    <automated>npm --prefix backend run build:worker</automated>
    <automated>npm --prefix backend run build:docker</automated>
    <automated>npm --prefix backend run build:netlify</automated>
    <automated>node scripts/validate_share_schema_alignment.js</automated>
    <automated>grep -F "PASS" .planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate_share_schema_alignment.js` exits `0`.
    - `.planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md` contains `PASS`.
    - `backend/dist/worker/worker.js` contains `share_links`.
    - `backend/dist/docker/server.js` contains `share_links`.
    - `backend/dist/netlify/api.mjs` contains `share_links`.
    - `npm --prefix backend test` exits `0`.
    - All three backend build commands exit `0`.
  </acceptance_criteria>
  <done>Blocking validation proves source, schema, migration, tests, and generated backend outputs are aligned across supported backend targets.</done>
</task>

<task type="auto">
  <name>Task 2: Record manual Drizzle push boundary and phase verification evidence</name>
  <files>.planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md</files>
  <read_first>
    - `.planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md`
    - `backend/package.json`
    - `.planning/phases/01-foundation-and-security-primitives/01-RESEARCH.md`
    - `.planning/codebase/STACK.md`
  </read_first>
  <action>Append a section named exactly `Manual Database Push Boundary` to `.planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md`. State that Phase 1 autonomous validation does not run `npx drizzle-kit push` because no live database credentials or Drizzle config file are present in this checkout. Include these exact documented manual command candidates for an operator with configured credentials: `npm --prefix backend run build:worker`, `npm --prefix backend run build:docker`, `npm --prefix backend run build:netlify`, and `npx --prefix backend drizzle-kit push`. Also append `Threats Covered` with the exact strings `raw token leakage`, `access-code guessing`, `fail-open rate limiting`, `deleted/revoked/expired share bypass`, `raw TOTP seed disclosure`, `unsafe logging/audit data`, `cache/referrer leakage`, `cross-owner access`, `generated-bundle drift`, and `cross-runtime schema drift`.</action>
  <verify>
    <automated>grep -F "Manual Database Push Boundary" .planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md</automated>
    <automated>grep -F "npx --prefix backend drizzle-kit push" .planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md</automated>
    <automated>grep -F "generated-bundle drift" .planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md && grep -F "cross-runtime schema drift" .planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md` contains `Manual Database Push Boundary`.
    - `.planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md` contains `npx --prefix backend drizzle-kit push`.
    - `.planning/phases/01-foundation-and-security-primitives/01-schema-build-validation.md` contains every threat string listed in the action.
  </acceptance_criteria>
  <done>Validation evidence documents the autonomous schema/build gate and the live-database Drizzle boundary without prompting for credentials.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source schema -> runtime migration | Drizzle schema and `migrator.ts` must describe the same share tables and columns. |
| source -> generated backend bundles | Build scripts must regenerate Worker, Docker, and Netlify outputs from the same source tree. |
| local validation -> live database push | Autonomous validation stops before live DB mutation because credentials/config are not present. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20 | Tampering | generated-bundle drift | mitigate | Run all three build scripts and validate `share_links`, `share_inaccessible`, and `share_item_inaccessible` in generated backend outputs. |
| T-01-21 | Tampering | cross-runtime schema drift | mitigate | `validate_share_schema_alignment.js` checks schema source, baseline SQL, migrator, and generated bundles for required share table and column strings. |
| T-01-22 | Information Disclosure | raw token leakage | mitigate | Validator checks generated outputs contain share primitives produced from source; Plan 02/03 tests verify raw values are not stored. |
| T-01-23 | Spoofing | access-code guessing | mitigate | Full test suite must include `shareRateLimitMiddleware.test.ts`, and validator requires `share_inaccessible` in generated outputs. |
| T-01-24 | Denial of Service | fail-open rate limiting | mitigate | Blocking gate runs middleware tests and build-output grep for fail-closed share limiter behavior. |
| T-01-25 | Elevation of Privilege | deleted/revoked/expired share bypass | mitigate | Blocking gate runs service tests and validates generated output contains `share_item_inaccessible`. |
| T-01-26 | Information Disclosure | raw TOTP seed disclosure | mitigate | Blocking gate preserves `SharedItemView` allowlist from source tests and documents the threat in validation evidence. |
| T-01-27 | Information Disclosure | unsafe logging/audit data | mitigate | Blocking gate runs repository/service tests and records audit/log threat coverage in validation evidence. |
| T-01-28 | Information Disclosure | cache/referrer leakage | mitigate | Blocking gate runs share security tests and records public response protection coverage. |
| T-01-29 | Elevation of Privilege | cross-owner access | mitigate | Blocking gate runs share service tests covering owner-accessible vault item checks. |
</threat_model>

<verification>
Run:

```bash
node scripts/restore_backend_source_from_sourcemaps.js --verify
npm ci --prefix backend
npm --prefix backend test
npm --prefix backend run build:worker
npm --prefix backend run build:docker
npm --prefix backend run build:netlify
node scripts/validate_share_schema_alignment.js
```
</verification>

<success_criteria>
- The blocking schema/migration validation requirement is satisfied with command evidence.
- The phase has a concrete PASS report or an exact BLOCKED report; there is no silent pass on missing source/build scripts.
- Generated Worker, Docker, and Netlify backend outputs are source-built and contain share schema/security primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-security-primitives/01-04-SUMMARY.md`.
</output>

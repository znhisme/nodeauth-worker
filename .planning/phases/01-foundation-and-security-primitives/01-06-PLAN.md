---
phase: 01-foundation-and-security-primitives
plan: 06
type: execute
wave: 6
depends_on: [05]
gap_closure: true
files_modified:
  - backend/dist/worker/worker.js
  - backend/dist/worker/worker.js.map
  - backend/dist/docker/server.js
  - backend/dist/docker/server.js.map
  - backend/dist/netlify/api.mjs
  - backend/dist/netlify/api.mjs.map
  - .planning/source-provenance.md
autonomous: true
requirements: [FND-01, FND-02, STATE-02, STATE-03, STATE-05]
must_haves:
  truths:
    - "Worker, Docker, and Netlify backend bundles are regenerated from the Plan 01-05 source fixes."
    - "Source maps verify that regenerated backend bundles still correspond to editable source."
    - "Full backend tests pass after generated output refresh."
    - "Share schema alignment validator passes after source and generated output refresh."
  artifacts:
    - path: "backend/dist/worker/worker.js"
      provides: "Regenerated Cloudflare Worker backend output"
      contains: "share_inaccessible"
    - path: "backend/dist/docker/server.js"
      provides: "Regenerated Docker backend output"
      contains: "share_inaccessible"
    - path: "backend/dist/netlify/api.mjs"
      provides: "Regenerated Netlify backend output"
      contains: "share_inaccessible"
    - path: ".planning/source-provenance.md"
      provides: "Source-map provenance refreshed if restore verification updates it"
      contains: "Generated bundles are not primary implementation files"
  key_links:
    - from: "src/features/share/shareService.ts"
      to: "backend/dist/worker/worker.js"
      via: "npm --prefix backend run build:worker"
      pattern: "share_inaccessible"
    - from: "src/features/share/shareService.ts"
      to: "backend/dist/docker/server.js"
      via: "npm --prefix backend run build:docker"
      pattern: "share_inaccessible"
    - from: "src/features/share/shareService.ts"
      to: "backend/dist/netlify/api.mjs"
      via: "npm --prefix backend run build:netlify"
      pattern: "share_inaccessible"
    - from: "backend/dist/*/*.map"
      to: "src/**"
      via: "scripts/restore_backend_source_from_sourcemaps.js --verify"
      pattern: "Source maps used"
---

<objective>
Refresh generated backend outputs and run blocking validation after Plan 01-05 source security fixes.

Purpose: Phase 1 cannot safely advance to route work unless the Cloudflare Worker, Docker, and Netlify bundles are regenerated from source and source-map/schema/test validation proves the distributable outputs match the fixed primitives.

Output: Regenerated backend dist outputs, refreshed provenance if source-map verification updates it, and green full validation commands for Phase 1 gap closure.
</objective>

<execution_context>
@/home/znh/.codex/get-shit-done/workflows/execute-plan.md
@/home/znh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-security-primitives/01-VERIFICATION.md
@.planning/phases/01-foundation-and-security-primitives/01-REVIEW.md
@.planning/phases/01-foundation-and-security-primitives/01-05-SUMMARY.md
@docs/share-link-security-contract.md
@backend/package.json
@backend/scripts/build-worker.js
@backend/scripts/build-docker.js
@backend/scripts/build-netlify.js
@scripts/restore_backend_source_from_sourcemaps.js
@scripts/validate_share_schema_alignment.js
@.planning/source-provenance.md

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 1 | Maintainer can safely implement share links from source, with durable share state and security primitives below the route layer. | 01-06 | COVERED | Proves Plan 01-05 source fixes are reflected in distributable backend targets.
REQ | FND-01 | Identify editable backend source, schema source, build scripts, and tests without hand-patching generated bundles. | 01-06 | COVERED | Source-map verification confirms generated output still maps to editable source.
REQ | FND-02 | Reproducible backend build path for Worker, Docker, and Netlify outputs. | 01-06 | COVERED | Runs all three backend build commands.
REQ | STATE-02 | Store tokens/codes only as hashes or derived values after creation. | 01-06 | COVERED | Full tests and regenerated bundles carry the Plan 01-05 raw-token persistence fix.
REQ | STATE-03 | Enforce expiration, revocation, deleted-item checks, and inaccessible-share status in service/repository logic. | 01-06 | COVERED | Full tests and generated outputs include the Plan 01-05 public-decision and revoke fixes.
REQ | STATE-05 | Share-specific public token/code rate limiting fails closed. | 01-06 | COVERED | Generated backend outputs include the fail-closed derived-token limiter.
CONTEXT | Decision | Do not hand-patch generated backend bundles as the primary implementation path. | 01-06 | COVERED | Build scripts produce `backend/dist/**`; executor must not manually edit dist files.
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Worker, Docker, and Netlify backend outputs</name>
  <files>backend/dist/worker/worker.js, backend/dist/worker/worker.js.map, backend/dist/docker/server.js, backend/dist/docker/server.js.map, backend/dist/netlify/api.mjs, backend/dist/netlify/api.mjs.map</files>
  <read_first>
    - backend/package.json
    - backend/scripts/build-worker.js
    - backend/scripts/build-docker.js
    - backend/scripts/build-netlify.js
    - .planning/phases/01-foundation-and-security-primitives/01-05-SUMMARY.md
  </read_first>
  <action>
    Regenerate backend outputs from the fixed source produced by Plan 01-05. Run `npm --prefix backend run build:worker`, `npm --prefix backend run build:docker`, and `npm --prefix backend run build:netlify` from the repository root. Do not hand-patch `backend/dist/**`; if a build fails, fix the source/build-script cause rather than editing generated output directly.

    After the builds, confirm the generated targets include the updated share primitive behavior by grepping for stable strings that should survive bundling, including `share_inaccessible`, `Cache-Control`, and `Referrer-Policy`.
  </action>
  <verify>
    <automated>npm --prefix backend run build:worker</automated>
    <automated>npm --prefix backend run build:docker</automated>
    <automated>npm --prefix backend run build:netlify</automated>
    <automated>rg -n "share_inaccessible|Cache-Control|Referrer-Policy" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `backend/dist/worker/worker.js` exists and contains `share_inaccessible`.
    - `backend/dist/docker/server.js` exists and contains `share_inaccessible`.
    - `backend/dist/netlify/api.mjs` exists and contains `share_inaccessible`.
    - `rg -n "Cache-Control|Referrer-Policy" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs` returns matches for all three generated targets.
    - `git diff -- backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs` reflects build output from `npm --prefix backend run build:*`; no task action instructs hand-patching `backend/dist/**`.
  </acceptance_criteria>
  <done>Worker, Docker, and Netlify generated backend outputs exist, are build-script produced, and contain the fixed share security primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Run source-map, test, and schema alignment validation</name>
  <files>.planning/source-provenance.md, backend/dist/worker/worker.js.map, backend/dist/docker/server.js.map, backend/dist/netlify/api.mjs.map</files>
  <read_first>
    - scripts/restore_backend_source_from_sourcemaps.js
    - scripts/validate_share_schema_alignment.js
    - .planning/source-provenance.md
    - backend/vitest.config.ts
  </read_first>
  <action>
    Run the blocking validation commands after generated output refresh:
    - `node scripts/restore_backend_source_from_sourcemaps.js --verify`
    - `npm --prefix backend test`
    - `node scripts/validate_share_schema_alignment.js`

    If source-map verification requires refreshing `.planning/source-provenance.md`, update only provenance statements that describe the current generated source-map state. Preserve the existing API-only frontend scope and the rule that generated bundles are not primary implementation files.
  </action>
  <verify>
    <automated>node scripts/restore_backend_source_from_sourcemaps.js --verify</automated>
    <automated>npm --prefix backend test</automated>
    <automated>node scripts/validate_share_schema_alignment.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/restore_backend_source_from_sourcemaps.js --verify` exits `0`.
    - `npm --prefix backend test` exits `0`.
    - `node scripts/validate_share_schema_alignment.js` exits `0`.
    - `.planning/source-provenance.md` still contains `Generated bundles are not primary implementation files`.
    - `.planning/source-provenance.md` still contains `Frontend source: not present; frontend/dist/** only`.
    - `backend/dist/worker/worker.js.map`, `backend/dist/docker/server.js.map`, and `backend/dist/netlify/api.mjs.map` exist after the rebuild.
  </acceptance_criteria>
  <done>Source maps verify, the full backend test suite passes, and schema/source/generated output alignment passes after the regenerated bundle refresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixed source -> generated backend bundles | Security fixes from Plan 01-05 cross into generated Worker, Docker, and Netlify artifacts consumed by deployments. |
| generated source maps -> editable source provenance | Source maps are used to prove generated outputs still correspond to editable source and are not hand-patched. |
| schema/source -> generated validation | Schema alignment checks compare committed schema source, migrator output, and generated backend strings across runtimes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | T | `backend/dist/**` generated outputs | mitigate | Rebuild Worker/Docker/Netlify bundles with package scripts after Plan 01-05; do not hand-patch generated files. |
| T-01-06-02 | T | source-map provenance | mitigate | Run `node scripts/restore_backend_source_from_sourcemaps.js --verify` and refresh `.planning/source-provenance.md` only if verification requires current source-map evidence. |
| T-01-06-03 | T | share schema alignment | mitigate | Run `node scripts/validate_share_schema_alignment.js` after generated output refresh to catch source/schema/bundle drift. |
| T-01-06-04 | D | backend test/build validation | mitigate | Run full `npm --prefix backend test` after build so generated-output refresh does not hide runtime or test regressions. |
</threat_model>

<verification>
Run these commands from the repository root after implementation:

```bash
npm --prefix backend run build:worker
npm --prefix backend run build:docker
npm --prefix backend run build:netlify
node scripts/restore_backend_source_from_sourcemaps.js --verify
npm --prefix backend test
node scripts/validate_share_schema_alignment.js
rg -n "share_inaccessible|Cache-Control|Referrer-Policy" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs
```
</verification>

<success_criteria>
Phase 1 generated-output gap closure is complete when:

- Worker, Docker, and Netlify backend bundles are regenerated from source after Plan 01-05.
- Source-map verification passes and provenance remains accurate.
- Full backend tests pass.
- Share schema alignment validation passes.
- Generated backend outputs contain the fixed share security behavior and are not hand-patched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-security-primitives/01-06-SUMMARY.md`.
</output>

---
phase: 02-share-link-api
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - backend/dist/worker/worker.js
  - backend/dist/worker/worker.js.map
  - backend/dist/docker/server.js
  - backend/dist/docker/server.js.map
  - backend/dist/netlify/api.mjs
  - backend/dist/netlify/api.mjs.map
autonomous: true
requirements:
  - OWN-01
  - OWN-02
  - OWN-03
  - OWN-04
  - OWN-05
  - OWN-06
  - OWN-07
  - REC-01
  - REC-02
  - REC-03
  - REC-04
  - REC-05
  - REC-06
  - UX-02
must_haves:
  truths:
    - "Worker, Docker, and Netlify backend bundles are regenerated from Phase 2 source changes."
    - "Generated outputs include the share route mount, owner/public route handlers, public header protections, and logger token redaction."
    - "Source maps still verify against restored `src/**` source after regeneration."
  artifacts:
    - path: "backend/dist/worker/worker.js"
      provides: "Cloudflare Worker generated backend with share API"
      contains: "/api/share"
    - path: "backend/dist/docker/server.js"
      provides: "Docker generated backend with share API"
      contains: "/api/share"
    - path: "backend/dist/netlify/api.mjs"
      provides: "Netlify generated backend with share API"
      contains: "/api/share"
  key_links:
    - from: "src/app/index.ts"
      to: "backend/dist/worker/worker.js"
      via: "npm --prefix backend run build:worker"
      pattern: "/api/share"
    - from: "src/app/index.ts"
      to: "backend/dist/docker/server.js"
      via: "npm --prefix backend run build:docker"
      pattern: "redactSharePublicToken"
    - from: "src/app/index.ts"
      to: "backend/dist/netlify/api.mjs"
      via: "npm --prefix backend run build:netlify"
      pattern: "share_inaccessible"
---

<objective>
Regenerate and validate backend distribution outputs for all supported deployment targets after Phase 2 API source changes.

Purpose: Keep Cloudflare Worker, Docker, and Netlify deployments aligned with restored TypeScript source without hand-editing generated bundles.
Output: Refreshed `backend/dist/**` generated files and validation evidence.
</objective>

<execution_context>
@/home/znh/.codex/get-shit-done/workflows/execute-plan.md
@/home/znh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-security-primitives/01-VERIFICATION.md
@.planning/phases/02-share-link-api/02-RESEARCH.md
@backend/package.json
@backend/scripts/build-worker.js
@backend/scripts/build-docker.js
@backend/scripts/build-netlify.js
@scripts/restore_backend_source_from_sourcemaps.js
@scripts/validate_share_schema_alignment.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full source-level Phase 2 validation before generating bundles</name>
  <files>src/features/share/shareService.test.ts, src/features/share/shareRoutes.test.ts, src/shared/middleware/shareRateLimitMiddleware.test.ts, src/app/index.test.ts</files>
  <read_first>
    - src/features/share/shareService.test.ts
    - src/features/share/shareRoutes.test.ts
    - src/shared/middleware/shareRateLimitMiddleware.test.ts
    - src/app/index.test.ts
    - backend/package.json
    - scripts/validate_share_schema_alignment.js
    - .planning/phases/02-share-link-api/02-RESEARCH.md
  </read_first>
  <action>
    Run the focused Phase 2 test set and the full backend test command before building generated outputs:
    `npm --prefix backend test -- src/features/share/shareService.test.ts src/features/share/shareRoutes.test.ts src/shared/middleware/shareRateLimitMiddleware.test.ts src/app/index.test.ts`
    and `npm --prefix backend test`.
    Because Phase 2 does not modify schema source, baseline SQL, or migration outputs, do not run a Drizzle push. Still run `node scripts/validate_share_schema_alignment.js` to confirm Phase 1 share schema remains aligned before generated output refresh.
    If any command fails, fix the source tests or source code before continuing to Task 2.
  </action>
  <verify>
    <automated>npm --prefix backend test -- src/features/share/shareService.test.ts src/features/share/shareRoutes.test.ts src/shared/middleware/shareRateLimitMiddleware.test.ts src/app/index.test.ts && npm --prefix backend test && node scripts/validate_share_schema_alignment.js</automated>
  </verify>
  <acceptance_criteria>
    - Command output for focused tests exits 0 and includes `shareService.test.ts`, `shareRoutes.test.ts`, `shareRateLimitMiddleware.test.ts`, and `index.test.ts`.
    - Command output for `npm --prefix backend test` exits 0.
    - Command output for `node scripts/validate_share_schema_alignment.js` exits 0.
    - `git diff --name-only -- src/shared/db/schema backend/schema.sql src/shared/db/migrator.ts` prints no paths.
  </acceptance_criteria>
  <done>All source tests and schema alignment validation pass before generated bundles are refreshed.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Worker, Docker, and Netlify backend bundles from source</name>
  <files>backend/dist/worker/worker.js, backend/dist/worker/worker.js.map, backend/dist/docker/server.js, backend/dist/docker/server.js.map, backend/dist/netlify/api.mjs, backend/dist/netlify/api.mjs.map</files>
  <read_first>
    - backend/package.json
    - backend/scripts/build-worker.js
    - backend/scripts/build-docker.js
    - backend/scripts/build-netlify.js
    - src/app/index.ts
    - src/features/share/shareRoutes.ts
    - src/features/share/shareService.ts
    - scripts/restore_backend_source_from_sourcemaps.js
  </read_first>
  <action>
    Rebuild generated backend outputs only through project scripts:
    `npm --prefix backend run build:worker`,
    `npm --prefix backend run build:docker`,
    and `npm --prefix backend run build:netlify`.
    Do not hand-edit `backend/dist/**`.
    After builds, run `node scripts/restore_backend_source_from_sourcemaps.js --verify`.
    Verify generated Worker, Docker, and Netlify bundles contain all of these strings: `/api/share`, `/public/:token/access`, `share_inaccessible`, `OwnerShareMetadataView` or `rawAccessCode`, `Cache-Control`, `Referrer-Policy`, and `redactSharePublicToken`.
  </action>
  <verify>
    <automated>npm --prefix backend run build:worker && npm --prefix backend run build:docker && npm --prefix backend run build:netlify && node scripts/restore_backend_source_from_sourcemaps.js --verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "/api/share|/public/:token/access|share_inaccessible|redactSharePublicToken" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs` finds all markers.
    - `rg -n "Cache-Control|Referrer-Policy|rawAccessCode" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs` finds public header and create handoff markers.
    - `node scripts/restore_backend_source_from_sourcemaps.js --verify` exits 0 after all three builds.
    - `git diff --name-only -- backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs` prints the three generated bundle paths.
  </acceptance_criteria>
  <done>All supported backend distribution outputs are regenerated from source and contain the mounted share API behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source -> generated bundles | TypeScript share API behavior is transformed into Worker, Docker, and Netlify runtime artifacts. |
| generated bundles -> deployment targets | Cloudflare, Docker, and Netlify run generated files instead of TypeScript source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | `backend/dist/**` | mitigate | Regenerate through `npm --prefix backend run build:*`; do not hand-edit generated bundles. |
| T-02-04-02 | Information Disclosure | Generated public route code | mitigate | Grep bundles for `share_inaccessible`, `Cache-Control`, `Referrer-Policy`, and `redactSharePublicToken` markers after build. |
| T-02-04-03 | Denial of Service | Source-map/build drift | mitigate | Run `node scripts/restore_backend_source_from_sourcemaps.js --verify` after all builds. |
| T-02-04-04 | Tampering | Schema drift | mitigate | Run `node scripts/validate_share_schema_alignment.js`; no schema push is required because Phase 2 does not modify schema files. |
</threat_model>

<verification>
Run the complete validation chain:

```bash
npm --prefix backend test -- src/features/share/shareService.test.ts src/features/share/shareRoutes.test.ts src/shared/middleware/shareRateLimitMiddleware.test.ts src/app/index.test.ts
npm --prefix backend test
node scripts/validate_share_schema_alignment.js
npm --prefix backend run build:worker
npm --prefix backend run build:docker
npm --prefix backend run build:netlify
node scripts/restore_backend_source_from_sourcemaps.js --verify
```

[BLOCKING] Schema push awareness: no schema push is required in Phase 2 because this plan set extends routes, services, repositories, tests, and generated bundles only. If execution discovers a need to modify `src/shared/db/schema/*.ts`, `backend/schema.sql`, `src/shared/db/migrator.ts`, or migration outputs, stop and add a blocking schema/migration validation task before continuing.
</verification>

<success_criteria>
Focused tests, full backend tests, share schema alignment validation, Worker/Docker/Netlify builds, and source-map verification all pass, and generated bundles include the Phase 2 share API markers.
</success_criteria>

<output>
After completion, create `.planning/phases/02-share-link-api/02-04-SUMMARY.md`
</output>

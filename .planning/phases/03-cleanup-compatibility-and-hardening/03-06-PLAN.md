---
phase: 03-cleanup-compatibility-and-hardening
plan: 06
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-04
  - 03-05
files_modified:
  - backend/dist/worker/worker.js
  - backend/dist/worker/worker.js.map
  - backend/dist/docker/server.js
  - backend/dist/docker/server.js.map
  - backend/dist/netlify/api.mjs
  - backend/dist/netlify/api.mjs.map
  - .planning/source-provenance.md
autonomous: true
requirements:
  - HARD-01
  - HARD-04
must_haves:
  truths:
    - "Full backend suite passes after Phase 3 cleanup, docs, schema, runtime, and hardening changes."
    - "Worker, Docker, and Netlify generated bundles are regenerated from source and include Phase 3 cleanup/schema/security markers."
    - "Source maps and provenance align after final bundle regeneration."
    - "Existing auth, vault, backup, health, and deployment behavior does not regress."
  artifacts:
    - path: "backend/dist/worker/worker.js"
      provides: "Final Worker generated backend"
      contains: "cleanupShareState"
    - path: "backend/dist/docker/server.js"
      provides: "Final Docker generated backend"
      contains: "cleanupShareState"
    - path: "backend/dist/netlify/api.mjs"
      provides: "Final Netlify generated backend"
      contains: "cleanupShareState"
    - path: ".planning/source-provenance.md"
      provides: "Source-map provenance after final regeneration"
      contains: "src/features/share/shareService.ts"
  key_links:
    - from: "src/**"
      to: "backend/dist/**"
      via: "backend build scripts only"
      pattern: "cleanupShareState|share_inaccessible|redactSharePublicToken|VARCHAR\\(255\\)"
---

<objective>
Run final regression validation and regenerate backend distribution artifacts from source.

Purpose: HARD-04 is complete only when existing NodeAuth behavior and all supported backend deployment bundles remain aligned after Phase 3.
Output: Passing full validation, regenerated backend bundles/source maps, and source-map provenance.
</objective>

<execution_context>
@/home/znh/.codex/get-shit-done/workflows/execute-plan.md
@/home/znh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-VALIDATION.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-02-SUMMARY.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-04-SUMMARY.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-05-SUMMARY.md
@backend/package.json
@backend/scripts/build-worker.js
@backend/scripts/build-docker.js
@backend/scripts/build-netlify.js
@scripts/restore_backend_source_from_sourcemaps.js
@scripts/validate_share_schema_alignment.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full regression and schema gates before final generation</name>
  <files>.planning/source-provenance.md</files>
  <read_first>
    - .planning/source-provenance.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-VALIDATION.md
    - scripts/validate_share_schema_alignment.js
    - backend/package.json
    - src/app/index.test.ts
    - src/features/share/shareService.test.ts
    - src/features/share/shareRoutes.test.ts
    - src/shared/middleware/shareRateLimitMiddleware.test.ts
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
  </read_first>
  <action>
    Run the full pre-generation validation gates:
    - `npm --prefix backend test`
    - `node scripts/validate_share_schema_alignment.js`
    - `node scripts/restore_backend_source_from_sourcemaps.js --verify`

    If source-map verification fails only because provenance is stale after source additions, run `node scripts/restore_backend_source_from_sourcemaps.js` to refresh `.planning/source-provenance.md`, then rerun `node scripts/restore_backend_source_from_sourcemaps.js --verify`. Do not edit generated bundles by hand.

    Confirm the full suite includes existing auth, vault, backup, health, app, and share tests available in this checkout. If any expected legacy test file is absent, record that absence in the summary; do not create placeholder tests.
  </action>
  <verify>
    <automated>npm --prefix backend test</automated>
    <automated>node scripts/validate_share_schema_alignment.js</automated>
    <automated>node scripts/restore_backend_source_from_sourcemaps.js --verify</automated>
  </verify>
  <acceptance_criteria>
    Full backend tests pass; schema alignment passes; source-map verification passes or provenance is refreshed and verification then passes.
  </acceptance_criteria>
  <done>Source-level regression gates are green before final generated bundle refresh.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and assert Worker Docker and Netlify outputs</name>
  <files>backend/dist/worker/worker.js, backend/dist/worker/worker.js.map, backend/dist/docker/server.js, backend/dist/docker/server.js.map, backend/dist/netlify/api.mjs, backend/dist/netlify/api.mjs.map, .planning/source-provenance.md</files>
  <read_first>
    - backend/scripts/build-worker.js
    - backend/scripts/build-docker.js
    - backend/scripts/build-netlify.js
    - scripts/restore_backend_source_from_sourcemaps.js
    - scripts/validate_share_schema_alignment.js
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-VALIDATION.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
  </read_first>
  <action>
    Regenerate final backend outputs only through:
    - `npm --prefix backend run build:worker`
    - `npm --prefix backend run build:docker`
    - `npm --prefix backend run build:netlify`

    Then run:
    - `node scripts/restore_backend_source_from_sourcemaps.js --verify`
    - `node scripts/validate_share_schema_alignment.js`
    - `npm --prefix backend test`

    Add generated-output assertions across `backend/dist/worker/worker.js`, `backend/dist/docker/server.js`, and `backend/dist/netlify/api.mjs` for:
    - `cleanupShareState`
    - `deleteStaleRateLimits`
    - `share_inaccessible`
    - `redactSharePublicToken`
    - `resolveApiCorsOrigin`
    - `VARCHAR(255)`
    - `Cache-Control`
    - `Referrer-Policy`

    If provenance verification fails because generated source maps changed, refresh `.planning/source-provenance.md` with `node scripts/restore_backend_source_from_sourcemaps.js`, then rerun verification. Do not modify `frontend/dist/**`.
  </action>
  <verify>
    <automated>npm --prefix backend run build:worker</automated>
    <automated>npm --prefix backend run build:docker</automated>
    <automated>npm --prefix backend run build:netlify</automated>
    <automated>node scripts/restore_backend_source_from_sourcemaps.js --verify</automated>
    <automated>node scripts/validate_share_schema_alignment.js</automated>
    <automated>npm --prefix backend test</automated>
    <automated>rg -n "cleanupShareState|deleteStaleRateLimits|share_inaccessible|redactSharePublicToken|resolveApiCorsOrigin|VARCHAR\\(255\\)|Cache-Control|Referrer-Policy" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs</automated>
  </verify>
  <acceptance_criteria>
    All three backend generated outputs contain Phase 3 cleanup, schema, generic error, CORS, header, and log-redaction markers; full suite and validator pass after regeneration.
  </acceptance_criteria>
  <done>Final generated backend outputs are source-built and verified across Worker, Docker, and Netlify.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source -> generated runtime bundles | Deployment uses generated backend artifacts; source fixes must be present in emitted code. |
| share feature -> existing NodeAuth features | Cleanup/runtime/schema edits must not regress auth, vault, backup, or health behavior. |
| generated source maps -> source provenance | Source-map drift can hide bundle/source mismatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T3-21 | Tampering | generated bundle drift | mitigate | Rebuild with project scripts only and grep emitted bundles for Phase 3 markers. |
| T3-22 | Denial of Service | auth/vault/backup/health regressions | mitigate | Run `npm --prefix backend test` before and after final bundle regeneration. |
| T3-23 | Tampering | source-map provenance drift | mitigate | Run `restore_backend_source_from_sourcemaps.js --verify` and refresh provenance only through the script if stale. |
| T3-24 | Information Disclosure | public error/header/log regressions | mitigate | Generated assertions require `share_inaccessible`, `Cache-Control`, `Referrer-Policy`, and `redactSharePublicToken`. |
| T3-25 | Denial of Service | MySQL migration regression | mitigate | Generated assertions require `VARCHAR(255)` and validator passes after final build. |
</threat_model>

<verification>
Run:
```bash
npm --prefix backend test
node scripts/validate_share_schema_alignment.js
npm --prefix backend run build:worker
npm --prefix backend run build:docker
npm --prefix backend run build:netlify
node scripts/restore_backend_source_from_sourcemaps.js --verify
rg -n "cleanupShareState|deleteStaleRateLimits|share_inaccessible|redactSharePublicToken|resolveApiCorsOrigin|VARCHAR\\(255\\)|Cache-Control|Referrer-Policy" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs
```
</verification>

<success_criteria>
- HARD-04 final regression gate passes.
- Worker, Docker, and Netlify generated bundles are aligned with source.
- Source-map verification and schema alignment validation pass after final builds.
</success_criteria>

<must_haves>
- Full backend test suite passes after all Phase 3 work.
- Generated backend bundles contain cleanup, schema, CORS, public error/header, and log-redaction markers.
- No direct hand-edits to `frontend/dist/**` or generated `backend/dist/**` are used as primary implementation.
</must_haves>

<output>
After completion, create `.planning/phases/03-cleanup-compatibility-and-hardening/03-06-SUMMARY.md`.
</output>

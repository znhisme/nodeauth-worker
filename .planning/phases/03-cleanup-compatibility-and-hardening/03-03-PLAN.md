---
phase: 03-cleanup-compatibility-and-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/share-link-security-contract.md
  - src/features/share/shareRoutes.ts
  - src/features/share/shareRoutes.test.ts
autonomous: true
requirements:
  - UX-03
must_haves:
  truths:
    - "Project docs say revocation blocks future access but cannot retract credentials already viewed or copied."
    - "Owner revoke API response carries non-secret wording that matches the docs/UI contract."
    - "No public recipient response distinguishes revoked shares from expired, wrong-code, locked, deleted, or missing-token states."
  artifacts:
    - path: "docs/share-link-security-contract.md"
      provides: "Revocation limitation contract"
      contains: "cannot retract"
    - path: "src/features/share/shareRoutes.ts"
      provides: "Owner revoke success message"
      contains: "cannot retract"
    - path: "src/features/share/shareRoutes.test.ts"
      provides: "Route contract tests for revoke wording and public generic errors"
      contains: "already viewed or copied"
  key_links:
    - from: "src/features/share/shareRoutes.ts"
      to: "docs/share-link-security-contract.md"
      via: "same revocation limitation language"
      pattern: "future access[\\s\\S]*cannot retract"
---

<objective>
Make the revocation limitation explicit in docs and owner API semantics without adding frontend work.

Purpose: UX-03 prevents misleading owners: revoking a link stops future link access, but it cannot pull back credentials a recipient already copied.
Output: Documentation and safe owner-facing API copy.
</objective>

<execution_context>
@/home/znh/.codex/get-shit-done/workflows/execute-plan.md
@/home/znh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-UI-SPEC.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-VALIDATION.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
@.planning/phases/02-share-link-api/02-02-SUMMARY.md
@docs/share-link-security-contract.md
@src/features/share/shareRoutes.ts
@src/features/share/shareRoutes.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add revocation limitation to owner API contract</name>
  <files>src/features/share/shareRoutes.ts, src/features/share/shareRoutes.test.ts</files>
  <read_first>
    - src/features/share/shareRoutes.ts
    - src/features/share/shareRoutes.test.ts
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-UI-SPEC.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
    - .planning/phases/02-share-link-api/02-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `DELETE /api/share/:id` success includes `message: 'Share link revoked. Future access is blocked, but NodeAuth cannot retract credentials already viewed or copied.'`.
    - Test 2: owner revoke response remains safe and does not include `rawToken`, `rawAccessCode`, `tokenHash`, `accessCodeHash`, `password`, `seed`, `otpauth`, `backup`, or `session`.
    - Test 3: `POST /api/share/public/:token/access` failure still returns exactly `{ success: false, message: 'share_inaccessible', data: null }` and does not mention revoked/copying details publicly.
  </behavior>
  <action>
    In `src/features/share/shareRoutes.ts`, change the owner revoke success message from `Share link revoked` to exactly:
    `Share link revoked. Future access is blocked, but NodeAuth cannot retract credentials already viewed or copied.`

    Keep the public recipient failure envelope exactly `{ success: false, message: 'share_inaccessible', data: null }`.

    Update `src/features/share/shareRoutes.test.ts` so owner revoke asserts the new message, and public recipient failure asserts the serialized body does not contain `revoked`, `expired`, `wrong-code`, `locked`, `deleted`, `already viewed`, `already copied`, or `cannot retract`.
  </action>
  <verify>
    <automated>npm --prefix backend test -- src/features/share/shareRoutes.test.ts</automated>
    <automated>rg -n "Future access is blocked, but NodeAuth cannot retract credentials already viewed or copied" src/features/share/shareRoutes.ts src/features/share/shareRoutes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Owner revoke success carries the limitation copy; public recipient errors remain generic; route tests cover both behaviors.
  </acceptance_criteria>
  <done>Owner API semantics are truthful about revocation without leaking public share state.</done>
</task>

<task type="auto">
  <name>Task 2: Update project revocation and cleanup notes</name>
  <files>docs/share-link-security-contract.md</files>
  <read_first>
    - docs/share-link-security-contract.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-UI-SPEC.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    In `docs/share-link-security-contract.md`, extend `## Revocation Semantics` with these exact statements:
    - `Revocation stops future access to the link.`
    - `Revocation cannot retract passwords, OTP codes, account details, or other credential data already viewed or copied by a recipient.`
    - `Owners should treat any previously accessed shared credentials as potentially copied outside NodeAuth's control.`

    Extend `## Expiration Policy` or add a short cleanup note with:
    - `Expired shares and stale access-attempt records may be cleaned up automatically. Cleanup does not change whether copied credentials remain usable outside NodeAuth.`

    Do not add UI implementation instructions. Do not edit `frontend/dist/**`.
  </action>
  <verify>
    <automated>rg -n "cannot retract|already viewed or copied|future access|potentially copied|Cleanup does not change" docs/share-link-security-contract.md src/features/share/shareRoutes.ts src/features/share/shareRoutes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Docs contain the exact UX-03 limitation language and cleanup note; grep over docs and source finds `cannot retract`, `already viewed or copied`, and `future access`.
  </acceptance_criteria>
  <done>Project notes and API copy explain revocation limits without misleading owners.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner API -> owner UI copy | Owner-facing API text can create false security expectations. |
| public recipient API -> unauthenticated users | Public errors must remain generic and not reveal revoked/expired/wrong-code/deleted states. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T3-09 | Repudiation | revocation semantics | mitigate | Docs and owner revoke API state future access is blocked but copied credentials cannot be retracted. |
| T3-10 | Information Disclosure | public recipient errors | mitigate | Route tests assert public failures do not mention revocation, expiration, wrong-code, lockout, deletion, or copy-risk wording. |
| T3-11 | Information Disclosure | owner revoke response | mitigate | Reuse owner metadata safe response tests; route response omits raw tokens/codes, hashes, passwords, seeds, backup, and session data. |
</threat_model>

<verification>
Run:
```bash
npm --prefix backend test -- src/features/share/shareRoutes.test.ts
rg -n "cannot retract|already viewed or copied|future access" docs .planning src
```
</verification>

<success_criteria>
- UX-03 is visible in docs and owner API semantics.
- Public recipient behavior remains generic.
- No generated frontend assets are edited.
</success_criteria>

<must_haves>
- Docs include `cannot retract` and `already viewed or copied`.
- Owner revoke success message includes the revocation limitation.
- Public share failures continue to return only `share_inaccessible`.
</must_haves>

<output>
After completion, create `.planning/phases/03-cleanup-compatibility-and-hardening/03-03-SUMMARY.md`.
</output>

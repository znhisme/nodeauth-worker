---
phase: 03-cleanup-compatibility-and-hardening
plan: 08
type: execute
wave: 6
depends_on:
  - 03-07
files_modified:
  - src/shared/db/migrator.ts
  - scripts/validate_share_schema_alignment.js
  - src/shared/db/shareSchemaAlignmentValidator.test.ts
  - src/features/share/shareRoutes.ts
  - src/features/share/shareRoutes.test.ts
autonomous: true
gap_closure: true
requirements:
  - HARD-01
  - HARD-02
must_haves:
  truths:
    - "MySQL share_rate_limits.share_id is wide enough for the hashed share identifier and no longer relies on the old VARCHAR(36) limit."
    - "The share create route never forwards non-finite ttlSeconds or expiresAt values to ShareService.createShare()."
    - "Regression tests fail on the legacy MySQL width and on NaN create input, then pass with the widened column and finite-number gate."
  artifacts:
    - path: "src/shared/db/migrator.ts"
      provides: "MySQL share_rate_limits migration"
      contains: "share_id VARCHAR(255)"
    - path: "scripts/validate_share_schema_alignment.js"
      provides: "Schema validator for MySQL share migration width"
      contains: "share_id VARCHAR(255)"
    - path: "src/shared/db/shareSchemaAlignmentValidator.test.ts"
      provides: "Validator regression coverage for the widened share_id column"
      contains: "share_id VARCHAR(255)"
    - path: "src/features/share/shareRoutes.ts"
      provides: "Finite-number gate for create-share TTL inputs"
      contains: "Number.isFinite"
    - path: "src/features/share/shareRoutes.test.ts"
      provides: "NaN create-input regression coverage"
      contains: "NaN"
  key_links:
    - from: "src/features/share/shareRoutes.ts"
      to: "ShareService.createShare()"
      via: "finite ttlSeconds/expiresAt normalization before service call"
      pattern: "Number\\.isFinite"
    - from: "src/shared/db/migrator.ts"
      to: "scripts/validate_share_schema_alignment.js"
      via: "MySQL share_rate_limits share_id assertion"
      pattern: "share_id VARCHAR\\(255\\)"
---

<objective>
Close the remaining schema-width and malformed-expiration verification gaps without changing the public share contract.

Purpose: HARD-01 still fails on the MySQL `share_rate_limits.share_id` width, and the create route still needs to stop forwarding non-finite expiration values into share creation.
Output: Bounded MySQL migration + validator coverage, plus finite-number route input gating and regression tests.
</objective>

<execution_context>
@/home/znh/.codex/get-shit-done/workflows/execute-plan.md
@/home/znh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-VERIFICATION.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-REVIEW.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
@src/shared/db/migrator.ts
@scripts/validate_share_schema_alignment.js
@src/shared/db/shareSchemaAlignmentValidator.test.ts
@src/features/share/shareRoutes.ts
@src/features/share/shareRoutes.test.ts
@src/features/share/shareService.ts
@src/features/share/shareTypes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the MySQL share limiter identifier and lock the validator to it</name>
  <files>src/shared/db/migrator.ts, scripts/validate_share_schema_alignment.js, src/shared/db/shareSchemaAlignmentValidator.test.ts</files>
  <read_first>
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-VERIFICATION.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-REVIEW.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
    - src/shared/db/migrator.ts
    - scripts/validate_share_schema_alignment.js
    - src/shared/db/shareSchemaAlignmentValidator.test.ts
    - src/shared/db/schema/mysql.ts
  </read_first>
  <behavior>
    - Test 1: the MySQL share migration block requires `share_rate_limits.share_id` to be `VARCHAR(255) NOT NULL`.
    - Test 2: the validator fails when the MySQL share migration keeps `share_id VARCHAR(36)` or any `TEXT` variant.
    - Test 3: the validator passes once the widened `VARCHAR(255)` share_id string is present in the migration block and test fixtures.
  </behavior>
  <action>
    Update `src/shared/db/migrator.ts` so the MySQL `create_share_link_tables` migration defines `share_rate_limits.share_id VARCHAR(255) NOT NULL` instead of `VARCHAR(36)`.

    Update `scripts/validate_share_schema_alignment.js` so the MySQL share-migration assertions require `share_id VARCHAR(255) NOT NULL` and still reject legacy `TEXT` share-id definitions.

    Update `src/shared/db/shareSchemaAlignmentValidator.test.ts` so the fixed MySQL fixture uses `share_id VARCHAR(255) NOT NULL`, a legacy `VARCHAR(36)` fixture fails, and the validator continues to reject unbounded `TEXT` share IDs.
  </action>
  <verify>
    <automated>node scripts/validate_share_schema_alignment.js</automated>
    <automated>npm --prefix backend test -- src/shared/db/shareSchemaAlignmentValidator.test.ts</automated>
    <automated>rg -n "share_id VARCHAR\\(255\\)|share_id VARCHAR\\(36\\)|share_id TEXT" src/shared/db/migrator.ts scripts/validate_share_schema_alignment.js src/shared/db/shareSchemaAlignmentValidator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    The MySQL migration and validator now agree on `share_rate_limits.share_id VARCHAR(255) NOT NULL`, and the regression test fails on the old width.
  </acceptance_criteria>
  <done>MySQL share-rate-limit persistence can hold hashed limiter identifiers without truncation or schema drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop non-finite create-share timing input before the service call</name>
  <files>src/features/share/shareRoutes.ts, src/features/share/shareRoutes.test.ts</files>
  <read_first>
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-VERIFICATION.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-REVIEW.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
    - src/features/share/shareRoutes.ts
    - src/features/share/shareRoutes.test.ts
    - src/features/share/shareService.ts
    - src/features/share/shareTypes.ts
  </read_first>
  <behavior>
    - Test 1: a create-share request body that supplies `ttlSeconds: Number.NaN` and `expiresAt: Number.NaN` reaches `createShareForOwner()` with both fields normalized to `undefined`.
    - Test 2: a finite-number create-share request still passes the same numeric values through unchanged.
    - Test 3: the route response shape and public owner metadata remain unchanged while invalid numeric input is stripped before the service call.
  </behavior>
  <action>
    Update `src/features/share/shareRoutes.ts` so the create-share handler only forwards finite numeric values to `createShareForOwner()`. Use `Number.isFinite(...)` on `ttlSeconds` and `expiresAt`; when the value is not finite, pass `undefined` so `ShareService.createShare()` never sees `NaN`, `Infinity`, or `-Infinity`.

    Update `src/features/share/shareRoutes.test.ts` with a regression case that stubs the request body to return `ttlSeconds: Number.NaN` and `expiresAt: Number.NaN`, then asserts `createShareForOwner()` receives `undefined` for both fields. Keep at least one existing finite-number happy path so the owner create contract stays unchanged.
  </action>
  <verify>
    <automated>npm --prefix backend test -- src/features/share/shareRoutes.test.ts</automated>
    <automated>rg -n "Number\\.isFinite|NaN|ttlSeconds|expiresAt" src/features/share/shareRoutes.ts src/features/share/shareRoutes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Non-finite timing input is stripped before share creation, and a NaN regression test proves the service never receives malformed expiration values.
  </acceptance_criteria>
  <done>Create-share requests can no longer smuggle NaN expiration data into share state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner create payload -> share route | The route accepts request-body timing values before calling share creation. |
| MySQL migration -> persisted share limiter rows | The migration determines whether hashed share identifiers can be stored safely. |
| schema validator -> generated backend contract | The validator is the source check that keeps MySQL drift out of later builds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T3-33 | Tampering | `src/features/share/shareRoutes.ts` expiration input | mitigate | Normalize only finite `ttlSeconds` and `expiresAt` values before the service call, and add a NaN regression test so malformed input cannot create an invalid share state. |
| T3-34 | Denial of Service | MySQL `share_rate_limits.share_id` persistence | mitigate | Widen the MySQL column to `VARCHAR(255)` and lock the validator/test fixtures to that width so hashed identifiers are not truncated or rejected. |
| T3-35 | Information Disclosure | route response semantics during input cleanup | accept | The route must keep the same public response shape while stripping malformed timing input; no new secret-bearing fields or error distinctions are introduced. |
</threat_model>

<verification>
Run:
```bash
node scripts/validate_share_schema_alignment.js
npm --prefix backend test -- src/shared/db/shareSchemaAlignmentValidator.test.ts src/features/share/shareRoutes.test.ts
rg -n "share_id VARCHAR\\(255\\)|Number\\.isFinite|NaN" src/shared/db/migrator.ts scripts/validate_share_schema_alignment.js src/shared/db/shareSchemaAlignmentValidator.test.ts src/features/share/shareRoutes.ts src/features/share/shareRoutes.test.ts
```
</verification>

<success_criteria>
- MySQL share-rate-limit rows accept the hashed share identifier width used by the limiter.
- Create-share input no longer forwards NaN or infinite timing values to share creation.
- Source-level regression tests explicitly cover both fixes.
</success_criteria>

<must_haves>
- MySQL share migration uses `share_rate_limits.share_id VARCHAR(255) NOT NULL`.
- Validator and validator test reject the old `VARCHAR(36)` definition.
- The create-share route strips non-finite numeric timing input before calling the service.
- A NaN regression test proves the route behavior.
</must_haves>

<output>
After completion, create `.planning/phases/03-cleanup-compatibility-and-hardening/03-08-SUMMARY.md`.
</output>

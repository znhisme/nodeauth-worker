---
phase: 03-cleanup-compatibility-and-hardening
plan: 09
type: execute
wave: 7
depends_on:
  - 03-08
files_modified:
  - src/features/share/shareService.ts
  - src/features/share/shareService.test.ts
  - backend/dist/worker/worker.js
  - backend/dist/worker/worker.js.map
  - backend/dist/docker/server.js
  - backend/dist/docker/server.js.map
  - backend/dist/netlify/api.mjs
  - backend/dist/netlify/api.mjs.map
  - .planning/source-provenance.md
autonomous: true
gap_closure: true
requirements:
  - HARD-03
  - HARD-04
must_haves:
  truths:
    - "Owner revocations emit a revoked audit event through revokeShareForOwner() with safe metadata."
    - "Worker, Docker, and Netlify bundles are regenerated from source and preserve the public share error/header contract."
    - "Source maps and provenance stay aligned after regeneration, and the full backend validation gates still pass."
  artifacts:
    - path: "src/features/share/shareService.ts"
      provides: "Owner revocation audit emission"
      contains: "eventType: 'revoked'"
    - path: "src/features/share/shareService.test.ts"
      provides: "Revocation audit regression coverage"
      contains: "eventType: 'revoked'"
    - path: "backend/dist/worker/worker.js"
      provides: "Regenerated Worker bundle"
      contains: "share_inaccessible"
    - path: "backend/dist/docker/server.js"
      provides: "Regenerated Docker bundle"
      contains: "Cache-Control"
    - path: "backend/dist/netlify/api.mjs"
      provides: "Regenerated Netlify bundle"
      contains: "Referrer-Policy"
    - path: ".planning/source-provenance.md"
      provides: "Source-map provenance after regeneration"
      contains: "src/features/share/shareService.ts"
  key_links:
    - from: "src/features/share/shareService.ts"
      to: "src/shared/db/repositories/shareRepository.ts"
      via: "insertAuditEvent after revokeForOwner succeeds"
      pattern: "eventType:\\s*'revoked'"
    - from: "src/**"
      to: "backend/dist/**"
      via: "backend build scripts only"
      pattern: "eventType:\\s*'revoked'|share_inaccessible|Cache-Control|Referrer-Policy"
    - from: "backend/dist/**"
      to: ".planning/source-provenance.md"
      via: "source-map verification and restore script"
      pattern: "src/features/share/shareService.ts"
---

<objective>
Close the revocation-audit regression and finish the phase with regenerated, source-map-verified backend bundles.

Purpose: HARD-03 and HARD-04 stay incomplete until owner revocations are auditable and the Worker, Docker, and Netlify outputs are rebuilt from the updated source without regressing the public share contract.
Output: Revocation audit emission, refreshed backend bundles/source maps, and a clean validation pass.
</objective>

<execution_context>
@/home/znh/.codex/get-shit-done/workflows/execute-plan.md
@/home/znh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-VERIFICATION.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-REVIEW.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-08-PLAN.md
@src/features/share/shareService.ts
@src/features/share/shareService.test.ts
@src/shared/db/repositories/shareRepository.ts
@backend/package.json
@backend/scripts/build-worker.js
@backend/scripts/build-docker.js
@backend/scripts/build-netlify.js
@scripts/restore_backend_source_from_sourcemaps.js
@scripts/validate_share_schema_alignment.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit revoked audit events from owner revocation and prove it in tests</name>
  <files>src/features/share/shareService.ts, src/features/share/shareService.test.ts</files>
  <read_first>
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-VERIFICATION.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-REVIEW.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
    - src/features/share/shareService.ts
    - src/features/share/shareService.test.ts
    - src/shared/db/repositories/shareRepository.ts
    - src/features/share/shareTypes.ts
  </read_first>
  <behavior>
    - Test 1: `revokeShareForOwner()` inserts exactly one audit row with `eventType: 'revoked'` when the owner revokes an accessible share.
    - Test 2: the revocation audit metadata includes the same safe `revokedAt` timestamp shape already used by the owner-facing revoke path.
    - Test 3: the new audit path does not leak raw tokens, access codes, vault secrets, or owner/private data into the audit payload.
  </behavior>
  <action>
    Update `src/features/share/shareService.ts` so `revokeShareForOwner()` mirrors the audited revocation behavior already used by `revokeShare()`: after `revokeForOwner()` succeeds, call `shareRepository.insertAuditEvent()` with a fresh audit id, the revoked `shareId`, `eventType: 'revoked'`, `actorType: 'owner'`, `eventAt: now`, the owner id, null IP/user-agent hashes, and `metadata` built from `{"revokedAt": now}`.

    Update `src/features/share/shareService.test.ts` to assert that owner revocation now emits the revoked audit row exactly once and that the safe metadata shape matches the existing audit contract. Keep the existing revoke behavior and owner metadata response unchanged.
  </action>
  <verify>
    <automated>npm --prefix backend test -- src/features/share/shareService.test.ts</automated>
    <automated>rg -n "eventType: 'revoked'|insertAuditEvent|revokedAt" src/features/share/shareService.ts src/features/share/shareService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Owner revocation now leaves an auditable `revoked` event behind, and the regression test proves the service emits it.
  </acceptance_criteria>
  <done>Owner revocations are visible in the audit trail again without changing the public revoke response.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate backend bundles and verify the restored hardening surface</name>
  <files>backend/dist/worker/worker.js, backend/dist/worker/worker.js.map, backend/dist/docker/server.js, backend/dist/docker/server.js.map, backend/dist/netlify/api.mjs, backend/dist/netlify/api.mjs.map, .planning/source-provenance.md</files>
  <read_first>
    - backend/package.json
    - backend/scripts/build-worker.js
    - backend/scripts/build-docker.js
    - backend/scripts/build-netlify.js
    - scripts/restore_backend_source_from_sourcemaps.js
    - scripts/validate_share_schema_alignment.js
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-08-PLAN.md
    - src/features/share/shareService.ts
    - src/features/share/shareService.test.ts
  </read_first>
  <action>
    Rebuild the backend outputs only through the existing project scripts:
    - `npm --prefix backend run build:worker`
    - `npm --prefix backend run build:docker`
    - `npm --prefix backend run build:netlify`

    After the rebuild, run the source-map verifier. If it reports provenance drift because the regenerated maps changed, refresh `.planning/source-provenance.md` with `node scripts/restore_backend_source_from_sourcemaps.js`, then rerun `node scripts/restore_backend_source_from_sourcemaps.js --verify`.

    Run the phase regression gates after the rebuild:
    - `npm --prefix backend test`
    - `node scripts/validate_share_schema_alignment.js`

    Add generated-output assertions across `backend/dist/worker/worker.js`, `backend/dist/docker/server.js`, and `backend/dist/netlify/api.mjs` for:
    - `eventType: 'revoked'`
    - `share_inaccessible`
    - `Cache-Control`
    - `Referrer-Policy`

    Do not hand-edit `backend/dist/**` or `frontend/dist/**`.
  </action>
  <verify>
    <automated>npm --prefix backend run build:worker</automated>
    <automated>npm --prefix backend run build:docker</automated>
    <automated>npm --prefix backend run build:netlify</automated>
    <automated>node scripts/restore_backend_source_from_sourcemaps.js --verify</automated>
    <automated>npm --prefix backend test</automated>
    <automated>node scripts/validate_share_schema_alignment.js</automated>
    <automated>rg -n "eventType: 'revoked'|share_inaccessible|Cache-Control|Referrer-Policy" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs</automated>
  </verify>
  <acceptance_criteria>
    Worker, Docker, and Netlify outputs are rebuilt from source, provenance stays aligned, and the generated bundles still carry the public share contract markers after the revocation audit fix.
  </acceptance_criteria>
  <done>All supported backend runtime bundles are refreshed and verified after the final source fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner revocation path -> audit repository | Revocation must emit evidence without leaking secret material. |
| source -> generated runtime bundles | The final runtime artifacts must reflect the audited source changes. |
| source maps -> provenance file | Source-map drift can hide bundle/source mismatches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T3-36 | Repudiation | `src/features/share/shareService.ts` revocation flow | mitigate | Emit the `revoked` audit event from `revokeShareForOwner()` and add a regression test that asserts the audit insert happens exactly once. |
| T3-37 | Tampering | regenerated `backend/dist/**` bundles | mitigate | Rebuild Worker, Docker, and Netlify outputs only through project build scripts and grep the emitted bundles for the revocation/public-header markers. |
| T3-38 | Tampering | source-map provenance drift | mitigate | Verify source maps after regeneration and refresh `.planning/source-provenance.md` only through the restore script if verification reports stale provenance. |
| T3-39 | Information Disclosure | public share contract during rebuild | mitigate | Preserve `share_inaccessible`, `Cache-Control`, and `Referrer-Policy` markers in generated-output assertions so rebuilds cannot silently weaken the public response contract. |
</threat_model>

<verification>
Run:
```bash
npm --prefix backend test
node scripts/validate_share_schema_alignment.js
npm --prefix backend run build:worker
npm --prefix backend run build:docker
npm --prefix backend run build:netlify
node scripts/restore_backend_source_from_sourcemaps.js --verify
rg -n "eventType: 'revoked'|share_inaccessible|Cache-Control|Referrer-Policy" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs
```
</verification>

<success_criteria>
- Owner revocations are audited again.
- Worker, Docker, and Netlify bundles are regenerated from source and source-map verified.
- The public share error/header contract remains present in generated output after the rebuild.
</success_criteria>

<must_haves>
- `revokeShareForOwner()` writes a `revoked` audit event.
- Backend build outputs are regenerated from source only through project scripts.
- Source-map provenance remains aligned after regeneration.
- Public share markers (`share_inaccessible`, `Cache-Control`, `Referrer-Policy`) survive the rebuild.
</must_haves>

<output>
After completion, create `.planning/phases/03-cleanup-compatibility-and-hardening/03-09-SUMMARY.md`.
</output>

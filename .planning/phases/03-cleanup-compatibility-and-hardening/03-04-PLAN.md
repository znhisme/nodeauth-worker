---
phase: 03-cleanup-compatibility-and-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/db/migrator.ts
  - scripts/validate_share_schema_alignment.js
  - backend/dist/worker/worker.js
  - backend/dist/worker/worker.js.map
  - backend/dist/docker/server.js
  - backend/dist/docker/server.js.map
  - backend/dist/netlify/api.mjs
  - backend/dist/netlify/api.mjs.map
autonomous: true
requirements:
  - HARD-01
must_haves:
  truths:
    - "MySQL share runtime migration no longer creates indexed share identifiers as unbounded TEXT columns."
    - "Schema alignment validation fails if MySQL share migration regresses to indexed TEXT."
    - "Worker, Docker, and Netlify builds pass after schema/migration changes."
  artifacts:
    - path: "src/shared/db/migrator.ts"
      provides: "Cross-dialect share migration SQL"
      contains: "VARCHAR(255)"
    - path: "scripts/validate_share_schema_alignment.js"
      provides: "Schema/migration/generated alignment validator"
      contains: "MySQL share migration"
    - path: "backend/dist/docker/server.js"
      provides: "Generated Docker runtime migration output"
      contains: "VARCHAR(255)"
  key_links:
    - from: "src/shared/db/migrator.ts"
      to: "scripts/validate_share_schema_alignment.js"
      via: "validator checks MySQL migration block"
      pattern: "create_share_link_tables[\\s\\S]*VARCHAR\\(255\\)"
---

<objective>
Fix and block regressions in share schema/runtime migration compatibility.

Purpose: Phase 2 explicitly deferred the MySQL indexed-TEXT migration risk; Phase 3 must close it before final verification.
Output: MySQL-safe migration SQL, stronger schema validator, and required build validation.
</objective>

<execution_context>
@/home/znh/.codex/get-shit-done/workflows/execute-plan.md
@/home/znh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-RESEARCH.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-VALIDATION.md
@.planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
@.planning/phases/02-share-link-api/02-VERIFICATION.md
@src/shared/db/migrator.ts
@src/shared/db/schema/mysql.ts
@scripts/validate_share_schema_alignment.js
@backend/scripts/build-worker.js
@backend/scripts/build-docker.js
@backend/scripts/build-netlify.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix MySQL share migration and validator regression checks</name>
  <files>src/shared/db/migrator.ts, scripts/validate_share_schema_alignment.js</files>
  <read_first>
    - src/shared/db/migrator.ts
    - src/shared/db/schema/mysql.ts
    - scripts/validate_share_schema_alignment.js
    - .planning/phases/02-share-link-api/02-VERIFICATION.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-RESEARCH.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: validator exits non-zero if MySQL migration block contains `id TEXT PRIMARY KEY`.
    - Test 2: validator exits non-zero if MySQL migration block contains `vault_item_id TEXT`, `owner_id TEXT`, `token_hash TEXT`, `access_code_hash TEXT`, `share_id TEXT`, or `key TEXT PRIMARY KEY`.
    - Test 3: validator requires MySQL migration block to contain `id VARCHAR(36) PRIMARY KEY`, `vault_item_id VARCHAR(36) NOT NULL`, `owner_id VARCHAR(255) NOT NULL`, `token_hash VARCHAR(255) NOT NULL`, `access_code_hash VARCHAR(255) NOT NULL`, and `key VARCHAR(255) PRIMARY KEY`.
  </behavior>
  <action>
    In the `version: 13`, `name: 'create_share_link_tables'`, `mysql:` SQL block in `src/shared/db/migrator.ts`, replace unbounded share identifier `TEXT` columns with bounded varchar types matching `src/shared/db/schema/mysql.ts`:
    - `share_links.id VARCHAR(36) PRIMARY KEY`
    - `share_links.vault_item_id VARCHAR(36) NOT NULL`
    - `share_links.owner_id VARCHAR(255) NOT NULL`
    - `share_links.token_hash VARCHAR(255) NOT NULL`
    - `share_links.access_code_hash VARCHAR(255) NOT NULL`
    - `share_audit_events.id VARCHAR(36) PRIMARY KEY`
    - `share_audit_events.share_id VARCHAR(36) NOT NULL`
    - `share_audit_events.event_type VARCHAR(50) NOT NULL`
    - `share_audit_events.actor_type VARCHAR(50) NOT NULL`
    - `share_audit_events.owner_id VARCHAR(255) NOT NULL`
    - `share_audit_events.ip_hash VARCHAR(255)`
    - `share_audit_events.user_agent_hash VARCHAR(255)`
    - `share_audit_events.metadata LONGTEXT`
    - `share_rate_limits.key VARCHAR(255) PRIMARY KEY`
    - `share_rate_limits.share_id VARCHAR(36) NOT NULL`

    Keep numeric BIGINT columns as currently defined.

    In `scripts/validate_share_schema_alignment.js`, keep the existing CommonJS `readFileSync`/`existsSync`/`fail()` style from the validator analog. Parse or slice the `mysql:` block for migration version 13 and fail with a clear message if the block contains indexed share columns as unbounded `TEXT`. Also require the bounded varchar strings listed in the behavior block. Keep existing source and generated bundle checks.
  </action>
  <verify>
    <automated>node scripts/validate_share_schema_alignment.js</automated>
    <automated>rg -n "id VARCHAR\\(36\\) PRIMARY KEY|token_hash VARCHAR\\(255\\) NOT NULL|key VARCHAR\\(255\\) PRIMARY KEY" src/shared/db/migrator.ts scripts/validate_share_schema_alignment.js</automated>
    <automated>rg -n "id TEXT PRIMARY KEY|token_hash TEXT NOT NULL|key TEXT PRIMARY KEY" src/shared/db/migrator.ts && exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    MySQL share migration uses bounded varchar for indexed identifiers; validator blocks the old indexed TEXT pattern; current validator exits 0 against fixed source.
  </acceptance_criteria>
  <done>MySQL migration compatibility debt from Phase 2 is fixed in source and guarded by automation.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Run schema alignment and backend build validation</name>
  <files>backend/dist/worker/worker.js, backend/dist/worker/worker.js.map, backend/dist/docker/server.js, backend/dist/docker/server.js.map, backend/dist/netlify/api.mjs, backend/dist/netlify/api.mjs.map</files>
  <read_first>
    - src/shared/db/migrator.ts
    - scripts/validate_share_schema_alignment.js
    - backend/package.json
    - backend/scripts/build-worker.js
    - backend/scripts/build-docker.js
    - backend/scripts/build-netlify.js
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-VALIDATION.md
    - .planning/phases/03-cleanup-compatibility-and-hardening/03-PATTERNS.md
  </read_first>
  <action>
    This is a blocking schema/migration compatibility validation task. Run these exact commands after Task 1 and before any final phase verification:
    - `node scripts/validate_share_schema_alignment.js`
    - `npm --prefix backend run build:worker`
    - `npm --prefix backend run build:docker`
    - `npm --prefix backend run build:netlify`
    - `node scripts/validate_share_schema_alignment.js`
    - `node scripts/restore_backend_source_from_sourcemaps.js --verify`

    Do not hand-edit `backend/dist/**`; only the build scripts may update generated bundles and source maps. If Docker/MySQL/PostgreSQL live smoke support is available locally, run the project compose/database smoke path and record the command/result in the plan summary. If live DB services are unavailable, record that live smoke was skipped because local services or credentials were unavailable; this does not replace the required validator/build/source-map gate above.
  </action>
  <verify>
    <automated>node scripts/validate_share_schema_alignment.js</automated>
    <automated>npm --prefix backend run build:worker</automated>
    <automated>npm --prefix backend run build:docker</automated>
    <automated>npm --prefix backend run build:netlify</automated>
    <automated>node scripts/restore_backend_source_from_sourcemaps.js --verify</automated>
    <automated>rg -n "VARCHAR\\(255\\)|VARCHAR\\(36\\)|share_links|create_share_link_tables" backend/dist/worker/worker.js backend/dist/docker/server.js backend/dist/netlify/api.mjs</automated>
  </verify>
  <acceptance_criteria>
    Required schema alignment command passes; all three backend builds pass; source-map verification passes; generated bundles contain fixed share migration markers.
  </acceptance_criteria>
  <done>Schema/migration compatibility is validated before later hardening and final generated-output verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source migration -> MySQL runtime | Fresh MySQL deployments execute migration SQL, not only Drizzle schema definitions. |
| source -> generated bundles | Worker, Docker, and Netlify generated outputs must include fixed migration code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T3-12 | Denial of Service | MySQL migration v13 | mitigate | Replace indexed unbounded `TEXT` columns with bounded `VARCHAR` and validate with `node scripts/validate_share_schema_alignment.js`. |
| T3-13 | Tampering | generated bundle drift | mitigate | Rebuild all three backend targets through project scripts and run source-map verification. |
| T3-14 | Denial of Service | live DB smoke availability | accept | Live MySQL/PostgreSQL commands are conditional because local services/credentials may be unavailable; source validator and build gates remain mandatory. |
</threat_model>

<verification>
Run:
```bash
node scripts/validate_share_schema_alignment.js
npm --prefix backend run build:worker
npm --prefix backend run build:docker
npm --prefix backend run build:netlify
node scripts/restore_backend_source_from_sourcemaps.js --verify
```
</verification>

<success_criteria>
- HARD-01 MySQL migration compatibility debt is closed.
- Schema validator prevents indexed-TEXT regressions.
- Build validation runs before final phase verification.
</success_criteria>

<must_haves>
- `src/shared/db/migrator.ts` MySQL share block uses bounded `VARCHAR`.
- `scripts/validate_share_schema_alignment.js` fails on old MySQL indexed `TEXT` share definitions.
- Required build validation commands pass.
</must_haves>

<output>
After completion, create `.planning/phases/03-cleanup-compatibility-and-hardening/03-04-SUMMARY.md`.
</output>

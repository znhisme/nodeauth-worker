# Quick Task 260504-ne8: Deploy Local Docker With GitHub OAuth

**Date:** 2026-05-04
**Status:** In progress

## Goal

Run NodeAuth locally through Docker using SQLite storage and the provided NodeAuth license plus GitHub OAuth configuration.

## Tasks

1. Prepare local-only Docker configuration.
   - Files: `.env.docker.local`, `.env.docker.local.compose.yml`
   - Action: Store runtime secrets in ignored local files and override placeholder values from `docker-compose.yml`.
   - Verify: `docker compose ... config` resolves required environment keys.

2. Start and validate the Docker service.
   - Files: Docker runtime state only
   - Action: Pull or build the image, start `nodeauth`, and inspect logs/HTTP responses.
   - Verify: Container is running and `http://localhost:3000` returns the NodeAuth app.

3. Record outcome.
   - Files: `.planning/quick/260504-ne8-docker-nodeauth-github-oauth/260504-ne8-SUMMARY.md`, `.planning/STATE.md`
   - Action: Summarize commands, verification, and any required external OAuth callback setting.
   - Verify: GSD quick task is marked complete without committing local secrets.
